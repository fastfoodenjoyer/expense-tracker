# Expense Tracker

CLI и Telegram бот для анализа расходов на основе банковских PDF-выписок.

## Возможности

- **Импорт выписок** из T-Bank, Альфа-Банк, Яндекс Банк, Озон Банк
- **Автоматическая категоризация** транзакций по правилам
- **Миграция категорий** при добавлении новых правил
- **Отчёты**: сводка по категориям, топ расходов, список транзакций
- **Экспорт** в Excel и Google Sheets (с дедупликацией)
- **Telegram бот** для управления с телефона
- **Автоматические бэкапы** в Cloudflare R2 с уведомлениями
- **Фильтрация** внутрибанковских переводов

## Установка

### Требования

- Python 3.13+
- [uv](https://github.com/astral-sh/uv) (рекомендуется) или pip

### Установка с uv

```bash
git clone https://github.com/your-username/expense-tracker.git
cd expense-tracker
uv sync
```

### Установка с pip

```bash
git clone https://github.com/your-username/expense-tracker.git
cd expense-tracker
pip install -e .
```

## CLI Команды

### Импорт выписки

```bash
expense-tracker import statement.pdf
```

Автоматически определяет банк по содержимому PDF.

### Список транзакций

```bash
# Все транзакции
expense-tracker list

# С фильтром по категории
expense-tracker list --category Продукты

# С фильтром по дате
expense-tracker list --from 01.01.2025 --to 31.01.2025

# Ограничить количество
expense-tracker list --limit 20

# Включить внутренние переводы
expense-tracker list --include-internal-transfers
```

### Сводка по категориям

```bash
# За всё время
expense-tracker summary

# За конкретный месяц
expense-tracker summary --month 1 --year 2025
```

### Топ расходов

```bash
# Топ 10 (по умолчанию)
expense-tracker top

# Топ 20 по категории
expense-tracker top --limit 20 --category Рестораны
```

### Список категорий

```bash
expense-tracker categories
```

### Экспорт

```bash
# В Excel
expense-tracker export expenses.xlsx

# В Excel + Google Sheets
expense-tracker export expenses.xlsx --google-sheet SPREADSHEET_ID

# За конкретный месяц
expense-tracker export expenses.xlsx --month 1 --year 2025
```

## Telegram бот

### Настройка

1. Создайте бота через [@BotFather](https://t.me/BotFather)
2. Создайте файл `.env` в корне проекта (см. `.env.example`)
3. Запустите бота:

```bash
expense-tracker bot
```

### Переменные окружения

```env
# Обязательные
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Шифрование credentials в БД
ENCRYPTION_KEY=your-secure-random-string

# Админы для уведомлений (JSON или через запятую)
ADMIN_IDS=[123456789, 987654321]

# Ограничение доступа (пусто = все пользователи)
ALLOWED_USERS=123456789,987654321

# Cloudflare R2 для бэкапов
R2_ACCOUNT_ID=your_account_id
R2_ACCESS_KEY_ID=your_access_key
R2_SECRET_ACCESS_KEY=your_secret_key
R2_BUCKET_NAME=expense-tracker-backups
```

### Функции бота

| Кнопка | Описание |
|--------|----------|
| 📥 Добавить транзакции | Импорт PDF-выписки |
| 📊 Сводка по категориям | Расходы по категориям за период |
| 🔝 Топ расходов | Самые крупные траты |
| 📋 Транзакции | Список с фильтром и пагинацией |
| 📁 Экспорт Excel | Скачать файл |
| 📤 Google Sheets | Синхронизация с таблицей |
| 📑 Категории | Список доступных категорий |
| ⚙️ Настройки | Google Sheets настройки |

## Автоматические бэкапы

Бот автоматически создаёт бэкапы базы данных:

- **Ежедневно** в 00:00 по Москве (21:00 UTC)
- **При остановке** бота (graceful shutdown)
- **Локально** в `~/.expense-tracker/backups/`
- **В облако** Cloudflare R2 (если настроено)

### Cloudflare R2

1. Создайте bucket в [Cloudflare R2](https://dash.cloudflare.com/)
2. Создайте API Token с правами на R2
3. Добавьте переменные в `.env`:
   - `R2_ACCOUNT_ID`
   - `R2_ACCESS_KEY_ID`
   - `R2_SECRET_ACCESS_KEY`
   - `R2_BUCKET_NAME`

### Уведомления админов

Если настроен `ADMIN_IDS`, бот отправляет уведомления о:
- Успешном запуске бота
- Результатах ежедневного бэкапа
- Ошибках бэкапа
- Восстановлении базы из R2

## Google Sheets

### Настройка через бота

Каждый пользователь настраивает свой Google Sheets:

1. Создайте проект в [Google Cloud Console](https://console.cloud.google.com/)
2. Включите Google Sheets API
3. IAM → Service Accounts → Create Service Account
4. Keys → Add Key → Create new key → JSON
5. В боте: `/set_credentials` и отправьте скачанный JSON файл
6. В боте: `/set_spreadsheet <ID таблицы>`
7. Поделитесь таблицей с email сервисного аккаунта (Editor)

Credentials хранятся в базе данных в зашифрованном виде (AES через Fernet).

**Команды бота:**
- `/set_credentials` — загрузить JSON ключ сервисного аккаунта
- `/set_spreadsheet <ID>` — установить ID таблицы
- `/clear_credentials` — удалить credentials
- `⚙️ Настройки` — проверить текущие настройки

### Формат экспорта

| Дата | Время | Сумма | Категория | Описание | Карта | Банк |
|------|-------|-------|-----------|----------|-------|------|

При экспорте автоматически:
- Создаётся лист "Транзакции" если не существует
- Добавляются заголовки если отсутствуют
- Пропускаются дубликаты (нормализация чисел для корректного сравнения)

### CLI использование

Для CLI можно использовать файл credentials:

```bash
expense-tracker export data.xlsx --google-sheet ID --credentials ~/.expense-tracker/credentials.json
```

## Миграция категорий

При запуске бот автоматически проверяет транзакции в категориях "Переводы" и "Прочее" и перекатегоризирует их по текущим правилам. Это позволяет:

- Добавлять новые категории без ручной перекатегоризации
- Улучшать правила категоризации с автоматическим применением к существующим данным

## Деплой на Railway

### Подготовка

Проект включает `Dockerfile` и `railway.toml` для деплоя.

### Шаги

1. Создайте проект в [Railway](https://railway.app/)

2. Подключите GitHub репозиторий или используйте CLI:
   ```bash
   railway login
   railway up
   ```

3. Добавьте переменные окружения (Settings → Variables):
   ```
   TELEGRAM_BOT_TOKEN=your_bot_token
   ENCRYPTION_KEY=your-secure-random-string
   ADMIN_IDS=123456789
   ALLOWED_USERS=123456789

   # Для бэкапов (рекомендуется)
   R2_ACCOUNT_ID=...
   R2_ACCESS_KEY_ID=...
   R2_SECRET_ACCESS_KEY=...
   R2_BUCKET_NAME=expense-tracker-backups
   ```

4. Создайте Volume для базы данных (Settings → Volumes):
   - Mount path: `/data`

5. Деплой произойдёт автоматически

### Восстановление из R2

При первом запуске, если база данных отсутствует и R2 настроен, бот автоматически восстановит последний бэкап из облака.

## Структура проекта

```
expense-tracker/
├── expense_tracker/
│   ├── __init__.py
│   ├── cli.py              # CLI интерфейс (Typer)
│   ├── models.py           # Pydantic модели
│   ├── storage.py          # SQLite хранилище + миграции
│   ├── backup.py           # Бэкапы в R2
│   ├── crypto.py           # Шифрование credentials (Fernet)
│   ├── categorizer.py      # Категоризация по правилам
│   ├── reports.py          # Генерация отчётов
│   ├── exporter.py         # Экспорт Excel/Google Sheets
│   ├── parsers/
│   │   ├── __init__.py
│   │   ├── base.py         # Базовый парсер
│   │   ├── tbank.py        # T-Bank
│   │   ├── alfabank.py     # Альфа-Банк
│   │   ├── yandex.py       # Яндекс Банк
│   │   └── ozon.py         # Озон Банк
│   └── bot/
│       ├── __init__.py
│       ├── main.py         # Entry point + scheduler
│       ├── config.py       # Pydantic Settings
│       ├── keyboards.py    # Клавиатуры
│       ├── states.py       # FSM состояния
│       └── handlers/
│           ├── __init__.py
│           ├── start.py    # /start и меню
│           ├── import_pdf.py
│           ├── export.py
│           ├── reports.py
│           ├── categories.py
│           └── settings.py # Google credentials
├── pyproject.toml
├── Dockerfile
├── railway.toml
├── .env.example
└── README.md
```

## Категории

| Категория | Примеры |
|-----------|---------|
| Продукты | Пятёрочка, Перекрёсток, ВкусВилл, Лента |
| Рестораны | Яндекс Еда, Delivery Club, кафе, рестораны |
| Транспорт | Яндекс Такси, метро, РЖД, АЗС |
| Связь | МТС, Билайн, Мегафон, Tele2 |
| Развлечения | Кинопоиск, Ozon, Wildberries, Steam |
| Здоровье | Аптеки, клиники, стоматология |
| Одежда | Zara, H&M, Спортмастер |
| ЖКХ | Коммунальные услуги, ТСЖ, УК, ГСК |
| Переводы | СБП, переводы между счетами |
| Кэшбэк | Возврат кэшбэка |
| Наличные | Снятие в банкомате |
| Прочее | Всё остальное |

## Хранение данных

- **База данных**: `~/.expense-tracker/expenses.db` (SQLite)
- **Бэкапы**: `~/.expense-tracker/backups/`
- **Google credentials**: в БД (зашифрованы)

На Railway с Volume:
- **База данных**: `/data/.expense-tracker/expenses.db`
- **Бэкапы**: `/data/.expense-tracker/backups/`

## Лицензия

MIT
