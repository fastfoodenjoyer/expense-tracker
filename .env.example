# Telegram Bot Configuration
# Create a bot via @BotFather and get the token
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Optional: Google Sheets integration
# Create a spreadsheet and copy the ID from the URL
GOOGLE_SPREADSHEET_ID=spreadsheet_id_here

# Optional: Restrict access to specific users
# Comma-separated list of Telegram user IDs
# Leave empty to allow all users
ALLOWED_USERS=123456789,987654321

# ===========================================
# Railway Deployment Notes:
# ===========================================
# 1. Add TELEGRAM_BOT_TOKEN as environment variable in Railway
# 2. Create a Volume mounted at /data for database persistence
# 3. Optionally add GOOGLE_SPREADSHEET_ID and ALLOWED_USERS
