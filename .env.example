# ===========================================
# Telegram Bot Configuration
# ===========================================

# Required: Bot token from @BotFather
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Admin IDs for notifications (JSON array or comma-separated)
# These users receive backup notifications and error alerts
ADMIN_IDS=[123456789, 987654321]

# Restrict access to specific users (JSON array or comma-separated)
# Leave empty to allow all users
ALLOWED_USERS=123456789,987654321

# ===========================================
# Encryption (for storing sensitive data)
# ===========================================

# Key for encrypting user credentials in database
# Generate a secure key for production!
ENCRYPTION_KEY=change-this-to-a-secure-random-string

# ===========================================
# Google Sheets (CLI only, bot uses per-user settings)
# ===========================================

# For CLI export command only. Bot users set their own credentials.
# GOOGLE_SPREADSHEET_ID=spreadsheet_id_here

# ===========================================
# Cloudflare R2 for Backups (optional but recommended)
# ===========================================

# Get these from Cloudflare Dashboard -> R2 -> Manage R2 API Tokens
R2_ACCOUNT_ID=your_cloudflare_account_id
R2_ACCESS_KEY_ID=your_r2_access_key
R2_SECRET_ACCESS_KEY=your_r2_secret_key
R2_BUCKET_NAME=expense-tracker-backups

# ===========================================
# AI Providers (optional, for future use)
# ===========================================

# JSON array of AI providers with failover support
# AI_PROVIDERS='[{"name":"Provider1","type":"google","api_key":"key1"},{"name":"Provider2","type":"openai_compatible","api_key":"key2","base_url":"https://api.example.com"}]'

# ===========================================
# Railway Deployment Notes
# ===========================================
# 1. Add all required variables above in Railway Dashboard
# 2. Create a Volume mounted at /data for database persistence
# 3. Database will auto-restore from R2 on new deployments if configured
